import { Placeholder } from "@/components/ui";

export default function LoginPage() {
  return (
    <Placeholder
      registro="compartida"
      rec="REC-54 / REC-17"
      titulo="Login / Autenticación"
      descripcion="Punto de entrada único para Agente y Damnificado. El sistema detecta el rol al autenticar y redirige a la vista correcta. Incluye recuperación de contraseña; el damnificado accede por invitación."
    />
  );
}
